package App;

import java.util.List;

public class Renter extends User {
    public Renter(String first_name, String last_name, String password, String email) {
        super(first_name, last_name, password, email);
    }
}
