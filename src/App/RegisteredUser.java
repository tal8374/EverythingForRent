package App;

public class RegisteredUser extends User {

    public RegisteredUser(int user_id, String first_name, String last_name, String password, String email) {
        super(first_name, last_name, password, email);
    }
}

